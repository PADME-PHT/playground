import { NgxGraphFixDirective } from './ngx-graph-fix.directive';

describe('NgxGraphFixDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxGraphFixDirective();
    expect(directive).toBeTruthy();
  });
});
